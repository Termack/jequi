Implement granular config for uri and hosts
FIX MEMORY LEAK FROM RELOAD (make reload) (pmap `cat jequi.pid` | tail -n 1) (dlclose does not unload library from memory)
Add tests to jequi_go and server
Setup more examples:
 - some css and js in static_files
 - small api in go with sqlite
HTTP2
MAYBE: load plugins in runtime using https://crates.io/crates/abi_stable